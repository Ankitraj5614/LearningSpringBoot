package com.SpaceTech.practice.TimeStamp;

import org.springframework.boot.SpringApplication;
import org.springframework.context.ConfigurableApplicationContext;

import com.SpaceTech.FirstdemoprojectApplication;

public class BlogPostmain {
/*
 * public static void main(String[] args) {
		 ConfigurableApplicationContext context = SpringApplication.run(FirstdemoprojectApplication.class, args);
		BlogService bs = (BlogService) context.getBean(BlogService.class);
//		bs.saveBlogs();
		bs.updateBlogs(1);
	}

 */
}
