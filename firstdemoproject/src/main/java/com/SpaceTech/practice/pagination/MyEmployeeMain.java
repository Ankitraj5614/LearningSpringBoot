package com.SpaceTech.practice.pagination;

import org.springframework.boot.SpringApplication;
import org.springframework.context.ConfigurableApplicationContext;

import com.SpaceTech.FirstdemoprojectApplication;

public class MyEmployeeMain {
/*
 * public static void main(String[] args) {
		 ConfigurableApplicationContext context = SpringApplication.run(FirstdemoprojectApplication.class, args);
		
		MyEmploService my=(MyEmploService) context.getBean(MyEmploService.class);
		//my.saveMyEmployee();
	//	my.findAllMyEmployee(4, 5);
		my.findAllMyEmployee(0, 6, "name");
		
	}
 */
}
